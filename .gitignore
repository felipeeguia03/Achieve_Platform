# Referencias visuales locales con datos de un sistema real (ADR-006).
# Versionar únicamente derivados textuales anonimizados.
/docs/design-system-source.html
/docs/diseño/

# Supabase local — claves y estado de la instancia de desarrollo.
# `supabase/config.toml` y `supabase/migrations/` SÍ se versionan: son el
# entorno reproducible. Lo que no viaja son las claves generadas por instancia.
/supabase/.branches
/supabase/.temp
/supabase/seed.sql

# Dependencias y artefactos de build
/node_modules
/.next
/out
/build
/coverage
*.tsbuildinfo
next-env.d.ts

# Entorno y sistema
.env*
!.env.example
# Los `.example` son plantillas sin secretos: viajan, y son la documentación
# ejecutable de qué variables hace falta definir.
!*.example
.DS_Store
npm-debug.log*
.gstack/

# Configuración de sesión de Claude Code. **No es producto**: los permisos que
# habilita valen para la máquina de quien los aprobó, no para el repositorio.
# ⚠️ Angosto a propósito: si mañana el proyecto quiere versionar skills, comandos
# o agentes compartidos bajo `.claude/`, pueden entrar sin tocar esto.
.claude/settings.json
.claude/settings.local.json
